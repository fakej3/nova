# Development

`master` is the canonical Wulan branch and the source for production deployments.

Use short-lived feature branches for active work. Merge completed work into `master`; do not treat old feature branches as alternate versions of Wulan.
