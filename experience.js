const canvas=document.getElementById('field');const ctx=canvas.getContext('2d');const world=document.getElementById('world');const zone=document.getElementById('presenceZone');const conversation=document.getElementById('conversation');const form=document.getElementById('composer');const input=document.getElementById('input');const clock=document.getElementById('clock');const particles=[];let dpr=1,w=0,h=0,mouse={x:.5,y:.5},energy=0;
function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);particles.length=0;const n=Math.min(150,Math.floor(w*h/9000));for(let i=0;i<n;i++)particles.push({x:Math.random(),y:Math.random(),r:Math.random()*1.6+.25,a:Math.random()*.55+.08,s:Math.random()*.00025+.00005,p:Math.random()*Math.PI*2});}
function draw(t=0){ctx.clearRect(0,0,w,h);const cx=w*(.5+(mouse.x-.5)*.025),cy=h*(.54+(mouse.y-.5)*.018);const g=ctx.createRadialGradient(cx,cy,0,cx,cy,Math.min(w,h)*.42);g.addColorStop(0,'rgba(92,185,235,.075)');g.addColorStop(.35,'rgba(65,119,181,.025)');g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);for(const p of particles){p.p+=p.s*16;const x=p.x*w+Math.sin(p.p+t*.00015)*10,y=p.y*h+Math.cos(p.p+t*.00012)*8;const a=p.a*(.65+.35*Math.sin(p.p*2+t*.0007));ctx.beginPath();ctx.arc(x,y,p.r,0,Math.PI*2);ctx.fillStyle=`rgba(150,220,250,${a})`;ctx.fill();}const glow=ctx.createRadialGradient(cx,cy,0,cx,cy,70+energy*45);glow.addColorStop(0,`rgba(121,232,255,${.09+energy*.09})`);glow.addColorStop(1,'rgba(121,232,255,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(cx,cy,90,0,Math.PI*2);ctx.fill();energy*=.94;requestAnimationFrame(draw)}
function now(){clock.textContent=new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'});}now();setInterval(now,1000);resize();addEventListener('resize',resize);addEventListener('pointermove',e=>{mouse.x=e.clientX/w;mouse.y=e.clientY/h;zone.style.transform=`translate(-50%,-50%) translate(${(mouse.x-.5)*10}px,${(mouse.y-.5)*7}px)`});
function addMessage(text,user=false){const el=document.createElement('div');el.className='msg'+(user?' user':'');el.innerHTML=`<small>${user?'YOU':'WULAN'}</small><p></p>`;el.querySelector('p').textContent=text;conversation.appendChild(el);while(conversation.children.length>3)conversation.removeChild(conversation.firstChild);}
const replies=["I’m here. What are we building next?","I’m with you. Let’s make it feel alive, not like a dashboard.","I can see the field. Give me a direction and I’ll follow it."];
form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;addMessage(text,true);input.value='';energy=1;setTimeout(()=>addMessage(replies[Math.floor(Math.random()*replies.length)]),420)});document.getElementById('voice').addEventListener('click',()=>{energy=1;input.focus();input.placeholder='Speak to Wulan…'});document.addEventListener('keydown',e=>{if(e.key==='Escape')input.blur()});draw();
