const normalize=value=>Array.isArray(value)?[...new Set(value.filter(Boolean).map(String))]:[];
const clone=value=>JSON.parse(JSON.stringify(value??null));
const resolvePath=(value,path)=>path.split('.').reduce((current,key)=>current==null?undefined:current[key],value);
const resolveInput=(value,results)=>{
  if(typeof value==='string'&&value.startsWith('$result.')){const [,taskId,...path]=value.split('.');return clone(resolvePath(results[taskId],path));}
  if(Array.isArray(value))return value.map(item=>resolveInput(item,results));
  if(value&&typeof value==='object')return Object.fromEntries(Object.entries(value).map(([key,item])=>[key,resolveInput(item,results)]));
  return value;
};
const teamId=()=>`team_${Date.now()}_${Math.random().toString(36).slice(2,9)}`;

export class WulanAgentSupervisor{
  constructor(core,{maxCandidates=20,maxTeamSteps=12,maxTeamHistory=100}={}){
    if(!core)throw new TypeError('Agent supervisor requires Wulan core');
    this.core=core;this.maxCandidates=maxCandidates;this.maxTeamSteps=maxTeamSteps;this.maxTeamHistory=maxTeamHistory;this.teamRuns=[];
    try{const snapshot=core.persistence?.load?.();this.restore(snapshot?.teamRuns??[]);}catch{}
    core.capabilities?.register?.({id:'agent.coordinate',name:'Coordinate Agent Team',description:'Execute a serializable dependency graph across multiple capable Wulan agents, passing successful task results to dependent tasks.',risk:'write',permissions:['agent:write'],inputSchema:{definition:'object'},execute:async({definition}={},context={})=>this.coordinate(definition,{approvedCapabilities:context.approvedCapabilities??[]})});
    core.capabilities?.register?.({id:'agent.team.status',name:'Team Run Status',description:'Inspect durable multi-agent team runs and their checkpoints.',risk:'read',permissions:['agent:read'],execute:async({teamId:id}={})=>id?this.getTeam(id):this.recentTeams(20)});
    core.capabilities?.register?.({id:'agent.team.resume',name:'Resume Agent Team',description:'Resume an interrupted or partial durable multi-agent team from its last checkpoint.',risk:'write',permissions:['agent:write'],inputSchema:{teamId:'string'},execute:async({teamId:id}={},context={})=>this.resume(id,{approvedCapabilities:context.approvedCapabilities??[]})});
  }
  candidates(definition){const tasks=Array.isArray(definition?.tasks)?definition.tasks:[];if(!tasks.length)throw new TypeError('Dispatch requires at least one task');const required=normalize(tasks.map(task=>task?.capabilityId));return this.core.agentRuntime.list().map(agent=>{const capabilities=normalize(agent.capabilities);const missing=required.filter(id=>!capabilities.includes(id));const recent=this.core.agentRuntime.recent(50).filter(run=>run.agentId===agent.id);const failed=recent.filter(run=>['failed','partial'].includes(run.status)).length;const running=recent.filter(run=>run.status==='running').length;const statusPenalty=agent.status==='error'?5:agent.status==='active'||agent.status==='assigned'?2:0;const capabilityScore=required.length?((required.length-missing.length)/required.length)*10:0;const reliability=Math.max(0,5-failed);const score=missing.length?Number.NEGATIVE_INFINITY:capabilityScore+reliability-statusPenalty-running;return{agent,score,missing,required,running,failed};}).filter(candidate=>candidate.missing.length===0).sort((a,b)=>b.score-a.score).slice(0,this.maxCandidates);}
  choose(definition){const candidates=this.candidates(definition);if(!candidates.length)throw new Error('NO_AGENT_CAN_EXECUTE_TASK');return candidates[0];}
  async dispatch(definition,options={}){const chosen=this.choose(definition);const result=await this.core.agentRuntime.assign(chosen.agent.id,definition,options);return{agentId:chosen.agent.id,score:chosen.score,candidates:this.candidates(definition).map(({agent,score,missing,running,failed})=>({agentId:agent.id,score,missing,running,failed})),run:result};}
  #validate(definition){const tasks=Array.isArray(definition?.tasks)?definition.tasks:[];if(!tasks.length)throw new TypeError('Coordination requires at least one task');if(tasks.length>this.maxTeamSteps)throw new Error(`TEAM_TASK_LIMIT_EXCEEDED: maximum ${this.maxTeamSteps} steps`);const ids=new Set();for(const task of tasks){if(!task?.id||ids.has(task.id))throw new Error(`INVALID_TEAM_TASK_ID: ${task?.id??'missing'}`);if(!task.capabilityId)throw new Error(`TEAM_CAPABILITY_REQUIRED: ${task.id}`);if(task.agentId&&!this.core.agentRuntime.get(task.agentId))throw new Error(`UNKNOWN_TEAM_AGENT: ${task.agentId}`);ids.add(task.id);for(const dependency of task.dependsOn??[]){if(dependency===task.id||!tasks.some(candidate=>candidate.id===dependency))throw new Error(`INVALID_TEAM_DEPENDENCY: ${task.id} -> ${dependency}`);}}this.#assertAcyclic(tasks);return tasks;}
  #assertAcyclic(tasks){const map=new Map(tasks.map(task=>[task.id,task]));const visiting=new Set(),visited=new Set();const visit=id=>{if(visiting.has(id))throw new Error(`TEAM_TASK_CYCLE_DETECTED: ${id}`);if(visited.has(id))return;visiting.add(id);for(const dep of map.get(id).dependsOn??[])visit(dep);visiting.delete(id);visited.add(id);};for(const task of tasks)visit(task.id);}
  #persist(){try{this.core.persistence?.saveCore?.(this.core);}catch{}}
  #saveTeam(run){const index=this.teamRuns.findIndex(item=>item.id===run.id);if(index>=0)this.teamRuns[index]=clone(run);else this.teamRuns.push(clone(run));if(this.teamRuns.length>this.maxTeamHistory)this.teamRuns.splice(0,this.teamRuns.length-this.maxTeamHistory);this.#persist();}
  async coordinate(definition,{approvedCapabilities=[],signal}={}){this.#validate(definition);const run={id:teamId(),name:String(definition.name??'Wulan agent team'),definition:clone(definition),status:'running',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),results:{},tasks:definition.tasks.map(task=>({id:task.id,status:'pending',agentId:task.agentId??null,capabilityId:task.capabilityId,attempts:0}))};this.teamRuns.push(clone(run));this.#saveTeam(run);return this.#executeTeam(run,{approvedCapabilities,signal});}
  async #executeTeam(run,{approvedCapabilities=[],signal}={}){const tasks=run.definition.tasks;const state=new Map(run.tasks.map(task=>[task.id,task]));for(const task of state.values())if(task.status==='running')task.status='pending';let progress=true;while(progress){progress=false;if(signal?.aborted){run.status='interrupted';run.updatedAt=new Date().toISOString();this.#saveTeam(run);throw new Error('TEAM_COORDINATION_CANCELLED');}for(const task of tasks){const current=state.get(task.id);if(['success','failed','blocked'].includes(current.status))continue;const dependencies=task.dependsOn??[];if(dependencies.some(id=>['failed','blocked'].includes(state.get(id)?.status))){current.status='blocked';current.error='DEPENDENCY_NOT_SUCCESSFUL';run.updatedAt=new Date().toISOString();this.#saveTeam(run);progress=true;continue;}if(dependencies.some(id=>state.get(id)?.status!=='success'))continue;try{const chosen=task.agentId?{agent:this.core.agentRuntime.get(task.agentId),score:null}:this.choose({tasks:[task]});const agent=chosen.agent;if(!agent)throw new Error(`UNKNOWN_TEAM_AGENT: ${task.agentId}`);if(!normalize(agent.capabilities).includes(String(task.capabilityId)))throw new Error(`AGENT_CAPABILITY_DENIED: ${task.capabilityId}`);const resolvedInput=resolveInput(task.input??{},run.results);const assignment={name:`${run.name} / ${task.id}`,tasks:[{id:task.id,capabilityId:task.capabilityId,input:resolvedInput,expected:task.expected}]};current.status='running';current.agentId=agent.id;current.startedAt=new Date().toISOString();current.attempts+=1;run.updatedAt=new Date().toISOString();this.#saveTeam(run);const agentRun=await this.core.agentRuntime.assign(agent.id,assignment,{approvedCapabilities,signal});if(agentRun.status!=='completed')throw new Error(`TEAM_AGENT_TASK_${agentRun.status.toUpperCase()}`);run.results[task.id]=agentRun.result?.results?.[task.id]??agentRun.result??agentRun;current.result=clone(run.results[task.id]);current.status='success';current.finishedAt=new Date().toISOString();run.updatedAt=new Date().toISOString();this.#saveTeam(run);}catch(error){current.status=signal?.aborted?'interrupted':'failed';current.error=String(error?.message??error);current.finishedAt=new Date().toISOString();run.updatedAt=new Date().toISOString();run.status=current.status==='interrupted'?'interrupted':'partial';this.#saveTeam(run);progress=true;break;}progress=true;}if(tasks.every(task=>['success','failed','blocked'].includes(state.get(task.id).status)))break;}const taskStates=tasks.map(task=>state.get(task.id));const status=taskStates.every(task=>task.status==='success')?'completed':taskStates.some(task=>task.status==='failed')?'partial':taskStates.some(task=>task.status==='interrupted')?'interrupted':'blocked';run.status=status;run.tasks=taskStates;run.finishedAt=status==='completed'||status==='partial'||status==='blocked'?new Date().toISOString():null;run.updatedAt=new Date().toISOString();this.#saveTeam(run);if(status!=='interrupted')this.core.remember({type:'agent-team',content:`Agent team “${run.name}” finished with status ${status}. Steps: ${taskStates.map(task=>`${task.id}:${task.status}:${task.agentId??'none'}`).join(', ')}`,importance:.68,confidence:.9,tags:['agent-team',status],source:'agent-supervisor'});return clone(run);}
  async resume(id,{approvedCapabilities=[],signal}={}){const run=this.getTeam(id);if(!run)throw new Error(`UNKNOWN_TEAM_RUN: ${id}`);if(run.status==='completed')return clone(run);if(run.status==='running')run.status='interrupted';for(const task of run.tasks)if(['running','interrupted'].includes(task.status))task.status='pending';run.status='running';run.updatedAt=new Date().toISOString();this.#saveTeam(run);return this.#executeTeam(run,{approvedCapabilities,signal});}
  getTeam(id){const run=this.teamRuns.find(item=>item.id===id);return run?clone(run):null;}
  recentTeams(limit=20){return this.teamRuns.slice(-limit).reverse().map(clone);}
  restore(snapshot=[]){this.teamRuns=Array.isArray(snapshot)?snapshot.filter(item=>item?.id&&item?.definition&&Array.isArray(item.tasks)).slice(-this.maxTeamHistory).map(clone):[];return this.teamRuns.length;}
  status(){const agents=this.core.agentRuntime.list();return{agents:agents.map(agent=>({id:agent.id,name:agent.name,status:agent.status,capabilities:normalize(agent.capabilities)})),available:agents.filter(agent=>agent.status==='idle').length,runs:this.core.agentRuntime.stats(),teams:{count:this.teamRuns.length,running:this.teamRuns.filter(team=>team.status==='running').length,partial:this.teamRuns.filter(team=>team.status==='partial').length,completed:this.teamRuns.filter(team=>team.status==='completed').length}};}
}
